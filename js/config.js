// Fill these in with your real Supabase values (Settings -> API Keys).
// The anon/publishable key is SAFE to be public here — it's designed to be
// used in the browser, and your database's row-level security (set up in
// supabase/schema.sql) is what actually keeps student data protected.
//
// Your Anthropic API key is NOT put here — that one lives only in
// api/chat.js's server-side environment variable, never in a file the
// browser can read.

export const SUPABASE_URL = "https://YOUR-PROJECT-REF.supabase.co";
export const SUPABASE_ANON_KEY = "your-publishable-key";
